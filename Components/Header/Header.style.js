import { StyleSheet } from "react-native";

export default headerStyle = StyleSheet.create({
    logo: {
        width: 200,
        resizeMode: 'contain',
    },
    title: {
        fontSize: 16,
        color: "#ABABAB"
    }
});
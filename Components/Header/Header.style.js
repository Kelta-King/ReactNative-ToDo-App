import { StyleSheet } from "react-native";

export default headerStyle = StyleSheet.create({
    logo: {
        width: 200,
        // height: 100,
        resizeMode: 'contain',
    },
    title: {
        color: "#ABABAB"
    }
});